{
  "name": "chart-view-toss",
  "private": true,
  "version": "0.7.0",
  "type": "module",
  "packageManager": "npm@10.9.8",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "test": "node tests/ait_contract.cjs",
    "build": "npm test && vite build",
    "build:ait": "npm test && vite build && ait build",
    "deploy:ait": "ait deploy",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "@apps-in-toss/web-framework": "3.4.1",
    "lightweight-charts": "^4.1.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
